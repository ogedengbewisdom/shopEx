# ShopEx

This project was generated using [Angular CLI](https://github.com/angular/angular-cli) version 21.0.4.

## Development server

To start a local development server, run:

```bash
ng serve
```

You can click the cart to view you cart items before checkout

You can use the addToCart (+) button to increase the quantiy of a product and removeFromCart (-) to reduce the quantiy or remove product entirly from the cart.

clear cart with clear cart button

clicking on a product twice will increase the quantity of item instead of adding product with same id

```bash
ng generate component component-name
```
